{
  "scripts": {
    "start": "coffee --compile --output src/ lib/ && node src/index.js",
    "compile": "coffee -c index.coffee"
  },
  "devDependencies": {
    "coffeescript": "^2.7.0"
  },
  "type": "module",
  "dependencies": {
    "axios": "^1.15.2",
    "cheerio": "^1.0.0-rc.12"
  }
}
